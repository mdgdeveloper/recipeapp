import { Box, Input } from '@chakra-ui/react';

interface Props {

    
}

const Entrada = (props: Props) => {
    return (
        <Box
        
        >
            <Input 
            w='100%'
            
            />   
        </Box>
    )
}

export default Entrada
